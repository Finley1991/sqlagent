# Vite dev server: proxy /api and /ws to this backend (default http://127.0.0.1:8000)
# VITE_DEV_API_TARGET=http://127.0.0.1:8010

# Optional: call backend directly (skip dev proxy). Example for local backend on 8010:
# VITE_API_BASE=http://127.0.0.1:8010
